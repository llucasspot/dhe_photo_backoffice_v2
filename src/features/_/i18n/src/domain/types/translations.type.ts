export type Translations = {
  auth: {
    login: {
      title: string;
      subtitle: string;
      email: string;
      password: string;
      submit: string;
    };
    register: {
      title: string;
      subtitle: string;
      email: string;
      password: string;
      confirmPassword: string;
      submit: string;
    };
  };
  navigation: {
    dashboard: string;
    schools: string;
    projects: string;
    signIn: string;
    signUp: string;
    signOut: string;
  };
};
